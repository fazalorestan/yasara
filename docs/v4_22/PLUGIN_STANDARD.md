# Plugin Standard

Each plugin should contain: manifest, metadata, version, dependencies, feature flags, required permissions, required licenses, event registration, routes, services, tests and documentation.

Lifecycle: Discovered -> Validated -> Licensed -> Loaded -> Initialized -> Running -> Paused -> Disabled -> Unloaded.
