export type ChartOverlayContract = { indicator: string; enabled: boolean; overlay: boolean; defaultVisible: boolean };

export const yasaraChartOverlayContract: ChartOverlayContract = {
  indicator: "yasara",
  enabled: true,
  overlay: true,
  defaultVisible: true,
};
