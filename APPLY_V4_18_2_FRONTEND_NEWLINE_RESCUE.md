# YaSara v4.18.2 Frontend Newline Rescue

Extract into:

```text
D:\yasara_clean
```

Run:

```cmd
cd /d D:\yasara_clean
python scripts\repair_frontend_app_newlines_v4_18_2.py
python yasara.py start
```

This fixes literal `\nimport` corruption in `frontend/src/App.tsx` and `frontend/src/main.tsx`.
