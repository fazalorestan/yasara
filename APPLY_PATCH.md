# نصب

```cmd
cd /d D:\yasara_clean
python scripts\install_yasara_release_command.py
```

# اجرای Release کامل

```cmd
python yasara.py release
```

# پیام Commit دلخواه

```cmd
python yasara.py release -m "Sprint 48 real data integration"
```

# Commit بدون Push

```cmd
python yasara.py release --no-push
```
