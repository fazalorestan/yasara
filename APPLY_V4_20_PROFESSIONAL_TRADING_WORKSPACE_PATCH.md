# Apply YaSara v4.20 Professional Trading Workspace

Extract into:

```text
D:\yasara_clean
```

Run:

```cmd
cd /d D:\yasara_clean
python yasara.py patch
python yasara.py start
```

API:

```text
http://127.0.0.1:8000/api/v1/v4-20/trading-workspace/summary
```

Adds 4 tests.
