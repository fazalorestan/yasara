# YaSara Professional v1.0 — Clean Release Complete

This package excludes `.venv` and cache files, while restoring all release files required by the test suite.

## Recommended test

```cmd
cd /d <extracted-folder>\backend
scripts\install_yasara_clean.bat
```
